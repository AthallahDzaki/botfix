const dp = require('./dp');
const info = require('./info');
const self = require('./self');
const help = require('./help');
const invite = require('./inv.js');

module.exports = {
  dp, info, self, help, invite,
};
